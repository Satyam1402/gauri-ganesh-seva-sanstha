<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Event Notifications
    |--------------------------------------------------------------------------
    |
    | Address that receives a notification email for every new event
    | registration, in addition to the admin users who hold "manage
    | events". Optional — leave empty to notify users only.
    |
    */

    'admin_notification_email' => env('EVENT_ADMIN_EMAIL'),

];
